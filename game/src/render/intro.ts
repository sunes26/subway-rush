/**
 * 인트로 — **시간의 순수 함수**다.
 *
 * `crowd.ts` 의 `surgeAt` · `trainAt` 과 같은 모양으로 짰다. dt 를 누적해 상태를
 * 굴리면 프레임이 튈 때 연출이 같이 튀고, 무엇보다 **마지막 프레임이 스폰 포즈와
 * 정확히 일치한다**는 것을 보증할 수 없다. 그 일치가 이 인트로의 전부다 —
 * 어긋나면 조작권이 넘어오는 순간 화면이 한 번 튀고, 그게 보이면 컷신이 된다.
 * 순수 함수면 `poseAt(INTRO_MS)` 를 유닛 테스트로 못 박을 수 있다.
 *
 *
 * ■ 3인칭으로 시작해 1인칭으로 끝난다
 *
 * 버스 안의 사건은 **몸이 있어야 성립한다** — 놀라는 것, 휴대폰을 드는 것.
 * 1인칭은 리액션을 못 보여 준다. 반대로 마지막 질주는 조작권으로 이어져야 하므로
 * 1인칭이어야 한다. 그래서 시점이 한 번 바뀐다.
 *
 * **그 전환을 컷으로 하지 않는다.** 3인칭 카메라가 하차하는 주인공의 **머리 속으로
 * 밀고 들어가서** 그대로 1인칭이 된다(`SWAP_MS`). 거리가 0 이 되는 순간이 곧
 * 시점 전환이라 이음매가 아예 존재하지 않는다. 컷으로 끊으면 거기서 "연출이
 * 끝났다"가 읽히고, 그 순간 인트로는 게임이 아니라 앞에 붙은 영상이 된다.
 *
 *
 * ■ 무대 (부록 A · `data/world.ts`)
 *
 *   차도 y16~22 · 인도 y22~34 · 상가 y34~43 — 전부 +x(동)로 뻗는다
 *   버스   OBJ-01-BUS  x −65.3~−54.4 · y 19.1~21.7 · h3.2   ← 연석에 붙여 세운다
 *   스폰   (−58, 24)                                        ← 쉘터 지붕 아래
 *   역     OBJ-05 출입구 x −0.9~8.9                          ← 여기서 58m 동쪽
 *
 * **스폰이 곧 버스정류장이다.** 그래서 이 인트로는 "역까지 뛰어가는 영상"이 될 수
 * 없다 — 역까지 뛰어가는 것이 게임 본편이기 때문이다. 인트로가 담당하는 것은
 * 버스에서 내려 **뛰기 시작하는 순간까지**이고, 조작권은 첫 몇 걸음에서 넘어간다.
 *
 *
 * ■ 요(yaw) 규약 — `camera-rig.ts` 의 1인칭과 **같은 정의를 쓴다**
 *
 *   `camera.rotation.set(pitch, yaw − π/2, 0)` · YXZ 에서 시선의 월드 방향은
 *   (cos yaw, sin yaw) 다. 즉 **yaw 0 = 동쪽(역 방향) · yaw +π/2 = 북쪽(인도 쪽)**.
 *   인트로가 자기 규약을 따로 만들면 마지막 프레임에서 반드시 어긋난다.
 */

import { clamp01, easeInOut, easeOutCubic, lerp } from '../core/math'
import { FPV } from '../data/tuning'
import { SPAWN } from '../data/world'
import { BUS, DOOR_X } from './bus-interior'
import { SIT_DROP } from './pose'

/** 샷 경계(ms). 합이 6.6초 — 브리프의 6~8초 안이다 */
/**
 * 네 개의 샷. **경계는 컷이다** — 그 사이를 카메라가 떠다니지 않는다.
 *
 * 이전 판은 어깨 오프셋 하나로 주인공을 계속 따라다녔다. 그게 "자유 카메라처럼
 * 움직인다"의 정체였다 — 촬영이 아니라 관찰이었고, 그래서 어느 한 프레임도
 * 구도가 잡히지 않았다. 이제 각 샷은 **자기 자리에 서서** 아주 조금만 민다.
 */
/**
 * ★ ② 가 길다 — **이 인트로가 전달해야 하는 정보가 거기 하나뿐이다.**
 *
 * 자막이 없으므로 "3분 후에 열차가 온다"를 아는 길은 폰 화면밖에 없다. 그런데
 * 1.4초짜리 컷에서는 밀고 들어가는 데만 시간이 다 가서, 글자가 읽을 만한 크기가
 * 되자마자 컷이 끝났다. 컷을 2.1초로 늘리고 **밀기는 앞쪽 0.9초에 몰았다**
 * (아래 push-in 참고) — 뒤 1.2초는 크기 그대로 멈춰 있는 시간이다.
 *
 * 나머지 셋은 길이를 안 건드렸다. ②만 +700ms 뒤로 밀리고 그 뒤가 통째로 따라간다.
 */
export const SHOT = {
  /** ① 버스 실내 미디엄 — 앉아 있다 */
  interior: 1400,
  /** ② 휴대폰 OTS — 어깨 너머로 화면이 읽힌다. 넷 중 가장 길다 */
  phone: 3500,
  /** ③ 버스 외부 · 문 — 기존 버스에서 내린다 */
  door: 4800,
  /** ④ 질주 팔로우 — 조작권이 넘어가는 곳 */
  dash: 6500,
} as const

/**
 * ② 안에서 **밀기가 끝나는** 시각. 이 뒤로는 카메라가 선 자리에 머문다.
 *
 * 밀기를 컷 전체(2.1초)에 펴면 글자가 계속 커지기만 하다가 끝난다 — 읽는 데
 * 필요한 것은 "충분히 큰 상태로 **머무는** 시간"이다. 0.9초에 붙고 1.2초를
 * 멈춘다.
 */
const PUSH_END = SHOT.interior + 900

export const INTRO_MS = SHOT.dash

/**
 * 카메라가 주인공의 머리에 도달해 1인칭이 되는 시각 = **인트로의 끝**.
 * 마지막 샷이 뒤에서 따라가다 그대로 눈으로 들어간다.
 */
export const SWAP_MS = INTRO_MS

/**
 * 버스가 완전히 멈추는 시각.
 *
 * 하차(SHOT 3)가 시작되는 3500ms 보다 **먼저**여야 한다. 브리프가 못 박은
 * *"움직이는 버스에서 뛰어내리는 것처럼 보이면 안 된다"* 가 이 한 줄이다.
 * 정지 후 180ms 를 비워 두어 "섰다"가 눈에 읽힌 뒤에 문이 열린다.
 *
 * ■ ★ 왜 컷보다 600ms 나 앞인가 — **③ 이 빈 버스로 시작했기 때문이다**
 *
 * 한동안 정차 3250 · 문 3660 · 하차 4000 이었다. 그러면 컷(3500) 직후 ③ 이
 * 열렸을 때 주인공은 아직 **좌석에서 문까지 걷는 중**이고, 그 걸음은 어두운
 * 실내에서 벌어져 밖에서는 안 보인다. 실측 캡처(4100ms)에 사람이 아예 없었다 —
 * 1.3초짜리 하차 샷의 앞 절반이 문만 열린 빈 버스였다.
 *
 * 컷을 늦출 수는 없다(② 를 길게 두기로 했다). 그래서 **사슬 전체를 앞으로
 * 당겼다** — 정차·문·기립이 ② 안에서 끝나고, ③ 은 이미 문이 열리고 사람이
 * 문간에 선 상태로 시작한다. 좌석→문 걸음은 컷이 삼킨다. 그게 원래 이 컷의
 * 역할이었다.
 */
export const BUS_STOP_MS = 2900

/**
 * 문이 열리기 시작하는 시각.
 *
 * ■ ★ **컷(3500) 뒤로 미뤘다** — 관객이 문이 열리는 것을 보게 한다
 *
 * 3080 이었다. 그러면 밖으로 나오는 컷 시점에 문이 이미 68% 열려 있어서, 화면에는
 * **열리는 과정이 안 남는다.** 하차 샷이 답해야 하는 것이 "이 버스에서 내렸다"인데,
 * 그 근거인 문 여닫힘을 정작 안 보여 주고 있었다.
 *
 * 3560 이면 컷 직후 60ms 에 열리기 시작한다 — 닫힌 문을 한 번 보여 준 뒤 열린다.
 * 완전히 열리는 것은 4180 이고 하차는 4120 에 시작하므로 94% 에서 발이 나간다.
 *
 * ⚠ 정차(2900)보다 660ms 뒤다. "선 뒤에 문이 열린다"는 순서는 유지된다.
 */
export const DOORS_MS = 3560

/**
 * 자리에서 일어서는 시각.
 *
 * ② 샷(1.4~3.5s) **안**이어야 한다. 컷 뒤에 일어나면 관객은 그 동작을 못 보고,
 * ③ 샷에서 밖에 나와 있는 주인공이 **순간이동**처럼 느껴진다. 일어나는 것까지
 * 보여 주고 문까지 걸어가는 사이를 컷으로 건너뛰는 것이 영화적 생략이다.
 *
 * ⚠ 다만 **컷 직전에 걸친다.** 2500 에 세웠더니 OTS 카메라(0.5m)를 흰 몸통이
 *   쓸고 지나가 화면 밝기가 한 프레임에 L135 → L156 으로 뛰었다 — 프레임 검사에
 *   그대로 걸렸다. 3300 이면 컷까지 200ms 라 "일어서기 시작"만 보이고 몸이
 *   렌즈를 덮기 전에 장면이 바뀐다.
 */
export const STAND_MS = 3300

/**
 * ■ 접근 거리 — **지면을 이어 붙여서 얻었다**
 *
 * Z1 의 지면은 x −64 에서 끝난다(`Z1-ROAD`·`Z1-WALK`). 그래서 한동안 1.2m 밖에
 * 못 달렸고, 창밖이 안 흘러서 "달리는 버스"가 아니라 "서 있는 버스"였다 —
 * 흔들림과 제동 쏠림만으로 버티고 있었다.
 *
 * `render/west-road.ts` 가 인트로 동안만 x −96 까지 32m 를 잇는다. 이제 24m 를
 * 달려 들어오고, 창밖으로 가로등·가로수·건물이 실제로 지나간다.
 *
 * ⚠ 이만큼 움직이면 **버스 외피를 인트로 초반에 숨겨야 한다.** 외피는 station GLB
 *   안에 붙박이로 서 있는데 실내만 24m 서쪽에서 다가오면, 우리가 탄 버스가
 *   저 앞에 주차된 다른 버스를 향해 달리는 그림이 된다(`main.ts` 참고).
 */
const APPROACH_M = 24

/**
 * 앉은 자리 — **북측(연석 쪽) 1인석**. 좌석 배열에서 나온 자리다.
 *
 * 열은 `IN.xW + 0.75` 에서 0.80m 간격이므로 −62.08 이 실제 좌석 중심이고,
 * 북측 1인석의 중심 y 는 21.22 다. 카메라에서 고른 자리가 아니라 **좌석이 있는
 * 자리**다 — 브리프의 순서(좌석 → 착석 → 휴대폰 → 카메라)를 그대로 따랐다.
 * 창(북)이 등 뒤에 오고, 문(−60.3)이 1.8m 동쪽이라 일어나 두 걸음이면 닿는다.
 */
/**
 * 앉은 자리. x 를 좌석 중심에서 **12cm 서쪽**으로 둔다 — 등이 등받이에 닿는다.
 *
 * ⚠ 문이 동쪽 베이로 옮겨가면서(`bus-interior.ts` 의 `DOOR`) 이 자리도 같이
 *   옮겼다. 북측 좌석 열은 0.80m 간격이고 문 칸은 비므로, 새 문(−58.9) 서쪽의
 *   첫 좌석은 **−60.45** 다(예전엔 −62.05). 문까지 1.55m 로 관계가 거의 같다.
 */
const SEAT = { x: -60.57, y: 21.20 } as const
/** 뒷문 안쪽 */
const DOORWAY = { x: DOOR_X, y: 21.05 } as const
/**
 * 연석을 밟고 내려선 자리 — 문 바로 앞 인도.
 *
 * ■ ★ y 를 22.6 → 23.25 로 올렸다 — **버스와의 거리 변화**가 스윕을 만든다
 *
 * 문을 동쪽으로 옮겨 동서 이동은 2.2m → 0.8m 로 줄었는데도 화면이 버스를 4.7m
 * 훑었다. 남은 원인은 **북쪽 간격**이다 — 하차 지점이 버스에서 0.87m 인데
 * 스폰(y 24)은 2.27m 라, 같은 화각으로 봐도 닿는 지점이 2.6배 멀어진다.
 * 카메라가 안 돌아도 훑는다.
 *
 * 22.6 은 "연석 바로 위"라는 뜻이었지만 그 대가가 이것이었다. 23.25 면 스폰과의
 * 차이가 0.75m 로 줄고, 여전히 버스에서 1.5m 라 "버스 옆에서 내렸다"는 읽힘은
 * 남는다(인도 폭은 y 22~34 라 한참 안쪽이다).
 */
const CURB = { x: DOOR_X + 0.1, y: 23.25 } as const

/**
 * 문을 향해 도는 각(76°). **정북(π/2)까지 안 돈다** — 화면 왼쪽 절반이 서쪽,
 * 즉 지도 밖이 되기 때문이다(위 `APPROACH_M` 주석과 같은 문제. 실측에서 왼쪽이
 * 통째로 하늘이었다). 76° 면 문을 향한 것으로 읽히면서 시야는 동쪽에 남는다.
 */
const DOOR_YAW = 1.32

export type IntroPose = Readonly<{
  /** 월드 x(동) */
  x: number
  /** 월드 y(북) */
  y: number
  /** 지면(z=0) 기준 눈높이 m */
  eye: number
  yaw: number
  pitch: number
  fov: number
}>

/** 0..1 구간 진행도 */
const seg = (t: number, a: number, b: number): number => clamp01((t - a) / (b - a))
/** `core/math` 에 없는 하나만 여기서 만든다 — 출발을 눌러 두는 3차 곡선 */
const easeIn = (u: number): number => clamp01(u) ** 3

/**
 * 버스의 위치 오프셋(m) — 정차 자리를 0 으로 두고 **음수에서 0 으로** 붙는다.
 * 실내·주인공·카메라가 전부 이 값을 함께 받는다. 하나라도 빠뜨리면 그것만
 * 버스 안에서 미끄러진다.
 */
export const busDx = (t: number): number =>
  -APPROACH_M * (1 - easeOutCubic(seg(t, 0, BUS_STOP_MS)))

/**
 * 노면 진동 — **이동거리로 못 낸 속도를 여기서 낸다.**
 *
 * 창밖이 흐르는 것만으로는 몸이 안 느낀다. 실제로 버스 안에서 속도를 느끼는
 * 경로의 절반은 **몸의 흔들림**이다. 두 개의 서로
 * 안 맞는 주파수를 겹쳐 규칙적인 진동이 되지 않게 하고, 제동과 **같은 곡선으로**
 * 잦아들게 한다 — 버스가 서면 흔들림도 같이 그친다. 그 동시성이 "이제 섰다"를
 * 자막 없이 말해 준다.
 */
export const busShake = (t: number): number => {
  const alive = 1 - easeOutCubic(seg(t, 0, BUS_STOP_MS))
  return (Math.sin(t / 61) * 0.55 + Math.sin(t / 23) * 0.45) * alive
}

/**
 * 제동 쏠림 — 완전히 서기 직전에 앞으로 쏠렸다가 돌아온다.
 *
 * 버스가 섰다는 것을 가장 확실하게 말해 주는 신체 감각이고, 이동 거리가 짧아도
 * 이것만은 그대로 느껴진다. `sin` 한 주기라 시작과 끝이 정확히 0 이다 — 잔여값이
 * 남으면 선 버스가 계속 기울어 있게 된다.
 */
const brakeDip = (t: number): number =>
  Math.sin(seg(t, BUS_STOP_MS - 400, BUS_STOP_MS + 260) * Math.PI * 2) * 0.055

// ─────────────────── 주인공 ───────────────────

export type ClipHint = 'Idle' | 'Walk' | 'Run'

export type ActorState = Readonly<{
  x: number
  y: number
  /** 지면(z=0) 기준 발 높이 — 버스 안에서는 바닥(0.9m) 위에 선다 */
  z: number
  /** 몸이 향하는 요 */
  facing: number
  clip: ClipHint
  /** 착석 정도 — 1 앉음 · 0 서 있음. `pose.ts` 가 받는다 */
  sit: number
  /** 휴대폰을 들어 본 정도 — 1 봄 · 0 내림 */
  phone: number
  /** 3인칭 구간에서만 보인다. 카메라가 머리에 닿으면 사라진다 */
  visible: boolean
}>

/**
 * 주인공의 자리.
 *
 * ■ ★ **앉는다.** 클립이 없어도 본을 직접 접으면 된다
 *
 * 이 자리에 한동안 정반대의 주석이 붙어 있었다 — *"★ 앉히지 않는다 … 손잡이를
 * 잡고 서 있는 것으로 간다"*. 근거는 `mc_character_rigged.glb` 에 앉은 클립이
 * 없다는 것이었고, **그 사실은 지금도 맞다**(Board·Hit·Idle·Jump·JumpAir·
 * JumpLand·Run·Slide·Sprint·Stumble·Walk 열한 종).
 *
 * 틀린 것은 결론이었다. 본을 열어 보니 17개뿐이고 이름이 붙어 있어서
 * **클립 없이 코드로 앉힐 수 있었다**(`render/pose.ts`). 그래서 `sit` 축이
 * 생겼고 주인공은 좌석에 앉아 있다. 그런데 주석은 안 고쳐져서, 코드를 읽는
 * 사람에게 **자신 있게 거짓말을 하고 있었다.**
 *
 * 남겨 두는 이유: 지금도 "앉은 클립이 없다"를 보고 같은 결론으로 갈 수 있다.
 * 없는 것은 클립이지 방법이 아니다.
 */
export const actorAt = (tMs: number): ActorState => {
  const t = Math.max(0, Math.min(INTRO_MS, tMs))
  const dx = busDx(t)

  /**
   * 앉음 → 일어섬 → 문으로 → 내려섬 → 질주.
   * 구간이 겹치지 않아야 "앉은 채로 걸어 나가는" 그림이 안 나온다.
   */
  const sit = 1 - seg(t, STAND_MS, STAND_MS + 300)
  /**
   * ⚠ 여기 시각은 **`SHOT` 에서 파생시킨다.** 한때 3280·3300 처럼 적어 뒀는데,
   *   ② 를 700ms 늘리자 `STAND_MS + 240`(3620)이 종료(3280)보다 뒤가 되어 걷는
   *   구간이 뒤집혔다. 유닛(`intro.test.ts`)이 잡았지만, 애초에 손으로 맞춰야
   *   하는 숫자를 안 두는 편이 낫다.
   */
  /**
   * ⚠ 하차 지점을 북쪽으로 올리면서(`CURB`) 문→연석 거리가 1.55 → 2.2m 가 됐다.
   *   시간을 그대로 두었더니 **8m/s** 가 되어 프레임 검사의 순간이동 항목에
   *   걸렸다(4349ms, 0.27m/프레임). 거리를 늘렸으면 시간도 늘려야 한다.
   *   두 걸음(좌석→문 1.67m · 문→연석 2.2m)을 3500~4700ms 에 나눠 담는다.
   */
  const toDoor = easeInOut(seg(t, STAND_MS + 200, SHOT.phone + 520))
  /**
   * 내려서는 구간. ③ 샷(3.5~4.8s) 안에서 **문 열림 → 하차**가 다 보여야 한다.
   *
   * ■ ★ 외피 유리가 주인공을 가린다 — **버스 안에서는 안 보인다**
   *
   * 한동안 하차를 컷 뒤 0.5s 에 시작했다. 그런데 4100ms 캡처에 사람이 없었다.
   * 위치를 재 보니 주인공은 화면 (855, 247) 에 **프레임 안**, `visible: true`
   * 였다 — 즉 가려진 것이다. 그 픽셀을 `__game.pick()` 으로 찍으니 답이 나왔다:
   *
   *   merged:BUS_GLASS   2.23m   ← 외피 유리
   *   MC_Character       2.64m   ← 주인공, 그 뒤
   *
   * **외피에는 문 구멍이 없다.** 위 `DOOR` 주석이 이미 적어 둔 사실인데
   * (*"외피에는 문이 없다 — 북측 유리가 끊김 없이 이어진다"*), 연출 타이밍이
   * 그걸 안 따졌다. 문은 실내 모듈이 만든 허구라 밖에서 열려 보이기는 해도,
   * 그 뒤를 걷는 사람은 유리에 막힌다. 몸 높이(z 1.65)는 유리 대역(1.24~2.43)
   * 한복판이고 그 아래는 파란 차체라 **버스 안에 있는 동안은 어떻게 해도 안 보인다.**
   *
   * ⚠ 그렇다고 **안 보이니까 순간이동시켜도 된다**는 뜻은 아니다. 한 번 그렇게
   *   100ms 에 옮겼다가 프레임 검사(`intro-frames.spec.ts`)의 순간이동 항목에
   *   걸렸다 — 1.08m 를 100ms 면 10m/s 다. 가려져 있다는 이유로 데이터를 뭉개면
   *   가림막이 걷히는 날 그대로 드러난다.
   *
   * 그래서 좌석→문은 **1.7m/s** 로 걷고(620ms · 유리 뒤라 안 보인다), 하차가
   * 그 뒤를 잇는다. 주인공은 4.3s 께 외피 북면(y 21.58)을 넘어 나오고 ③ 의
   * 마지막 500ms 와 ④ 전체를 화면에 남는다. 컷은 "문이 열리고, 사람이 나온다"
   * 순서를 그대로 담는다.
   */
  const down = seg(t, SHOT.phone + 520, SHOT.door - 100)
  const run = seg(t, SHOT.door, INTRO_MS)

  const inBusX = lerp(SEAT.x, DOORWAY.x, toDoor) + dx
  const inBusY = lerp(SEAT.y, DOORWAY.y, toDoor)
  const offX = lerp(inBusX, CURB.x, easeInOut(down))
  const offY = lerp(inBusY, CURB.y, easeInOut(down))

  return {
    // 내려선 뒤 스폰까지 달린다 — 조작권은 도착하는 순간 넘어간다
    x: lerp(offX, SPAWN.x, easeIn(run) * 0.3 + easeInOut(run) * 0.7),
    y: lerp(offY, SPAWN.y, easeInOut(run)),
    /**
     * 앉은 동안 리그 원점이 `SIT_DROP` 만큼 **내려간다.** 다리를 접는 것만으로는
     * 골반이 안 내려와 좌석 위에 서 있게 된다(`pose.ts` 참고).
     */
    z: lerp(lerp(BUS.floor, 0, easeIn(down)), 0, run) - sit * SIT_DROP,
    /**
     * 앉아서는 진행 방향(동, yaw 0)을 본다 — 좌석이 그쪽을 보고 있으니 당연하다.
     * 일어나면 문(북)으로 돌고, 내린 뒤엔 다시 동쪽(역)으로 튼다.
     */
    facing: lerp(
      lerp(0, DOOR_YAW, seg(t, STAND_MS, STAND_MS + 480)),
      0, easeOutCubic(run / 0.35),
    ),
    // 휴대폰은 ② 샷에서만 든다
    phone: seg(t, SHOT.interior - 180, SHOT.interior + 220)
      * (1 - seg(t, SHOT.phone - 260, SHOT.phone)),
    clip: run > 0 ? 'Run' : toDoor > 0 ? 'Walk' : 'Idle',
    sit,
    /**
     * ★ 카메라가 머리에 **닿기 전에** 감춘다.
     *
     * 마지막 프레임까지 켜 두었더니 카메라가 머리 속으로 들어가 **흰 덩어리가
     * 화면을 덮었다** — 녹화본에서 6.51s 프레임의 평균 밝기가 107 → 117 로 튀는
     * 그 지점이다. 페이드도 톤매핑도 아니고 **캐릭터 메시**였다.
     *
     * 300ms 앞서 끄면 그때 카메라는 아직 0.4m 뒤라 머리가 화면을 다 안 덮는다.
     * 그 뒤 300ms 는 이미 사실상 1인칭이라 없어진 것이 눈에 안 띈다.
     */
    /**
     * ★ **카메라가 머리에 닿았을 때** 사라진다 — 고정 시간이 아니다.
     *
     * `INTRO_MS − 300` 으로 박아 뒀을 때는 카메라가 아직 1.0m 뒤에 있었고,
     * 그 300ms 동안 **아무도 없는 거리**를 카메라가 쫓아가는 그림이 나왔다.
     * 오프셋이 92% 닫히면(≈0.15m) 몸이 렌즈를 덮으므로 그때 끈다.
     */
    visible: closeAt(t) < 0.92,
  }
}

// ─────────────────── 카메라 ───────────────────

/**
 * 마지막 포즈 = **1인칭이 스폰에서 잡는 포즈와 같은 값**.
 *
 * `camera-rig.ts` 의 1인칭 분기가 눈을 `(pos.x, pos.z + FPV.eyeHeight, −pos.y)` 에
 * 놓고 회전을 `(lookPitch, lookYaw − π/2, 0)` 으로 준다. 입력의 초기 시선은 0 이므로
 * 요·피치 모두 0 이고, 요 0 은 위 규약대로 **동쪽 = 역 방향**이다.
 */
export const FINAL_POSE: IntroPose = {
  x: SPAWN.x, y: SPAWN.y, eye: SPAWN.z + FPV.eyeHeight, yaw: 0, pitch: 0, fov: FPV.fovDeg,
}

/**
 * ④ 질주 팔로우의 뒤따르는 거리 — 주인공 기준 (동쪽, 북쪽).
 *
 * 동쪽으로 달리므로 e 는 음수(뒤)다. n 도 조금 음수라 **정후방이 아니라 3/4 후방**이
 * 된다 — 정중앙에서 멀리 따라가면 주인공만 크게 보이고 어디로 가는지가 안 읽힌다.
 *
 * ■ ★ n 을 −0.78 → −0.30 으로 줄였다 — **카메라가 버스에 붙어 있었다**
 *
 * 하차 직후 주인공은 y 22.6 에 있고 외피 북면은 y 21.58 이다. n 이 −0.78 이면
 * 카메라가 y 21.82, 즉 **버스에서 0.24m** 다. 그 거리에서 4m 를 동쪽으로 훑으면
 * 버스 옆면이 프레임을 통째로 쓸고 지나가면서 **문 옆 → 다른 구간**으로 화면이
 * 완전히 바뀐다. 실제로 "내린 버스가 다른 버스로 바뀐 것 같다"는 지적이 이것이었다.
 * 물건이 바뀐 게 아니라 **너무 가까이서 훑은 것**이다.
 *
 * −0.30 이면 시작 거리가 0.72m 로 세 배가 되어 같은 이동에도 각이 훨씬 덜 변한다.
 * 3/4 후방이라는 성격은 그대로 남는다.
 */
const FOLLOW = { e: -1.90, n: -0.30 } as const

/**
 * 팔로우 오프셋이 **얼마나 닫혔는가** — 0 이면 아직 뒤에서 따라가고, 1 이면
 * 카메라가 주인공의 눈에 도달했다(= 1인칭).
 *
 * ■ ★ 예전엔 마지막 **520ms** 안에 1.89m 를 다 닫았다
 *
 * 거기에 주인공의 전진 속도가 더해져 카메라가 순간 8m/s 로 튀었다(실측:
 * 6000→6400ms 구간에서 프레임당 0.43 → 1.02 → 1.34m). 컷이 아닌데 컷처럼 보이고,
 * 마침 그 직전에 주인공이 사라지므로 **빈 거리가 확 당겨지는** 그림이 된다.
 *
 * 이제 ③→④ 컷 직후부터 천천히 닫는다. 같은 1.89m 를 1.5초에 나눠 쓰므로
 * 최대 속도가 1/3 이하로 떨어지고, 마지막 프레임들은 이미 거의 도착해 있어
 * 조작권이 넘어오는 순간이 조용하다.
 */
const closeAt = (t: number): number =>
  easeInOut(seg(t, SHOT.door + 150, INTRO_MS))



/**
 * 샷 하나 = **고정된 자리에서 한 점을 본다.** 아주 조금만 민다(dolly).
 * 두 점을 주고 `u` 로 섞는 것이 곧 그 미는 양이다.
 */
const framed = (
  from: readonly [number, number, number],
  to: readonly [number, number, number],
  at: readonly [number, number, number],
  u: number,
  dx = 0,
): IntroPose => {
  const e = easeInOut(u)
  const x = lerp(from[0], to[0], e) + dx
  const y = lerp(from[1], to[1], e)
  const z = lerp(from[2], to[2], e)
  const ax = at[0] + dx
  const d = Math.hypot(ax - x, at[1] - y)
  return {
    x, y, eye: z,
    yaw: Math.atan2(at[1] - y, ax - x),
    pitch: Math.atan2(at[2] - z, d),
    fov: FPV.fovDeg,
  }
}

export const poseAt = (tMs: number): IntroPose => {
  const t = Math.max(0, Math.min(INTRO_MS, tMs))
  const a = actorAt(t)
  const shake = busShake(t)
  const dx = busDx(t)

  /**
   * ── ① 버스 실내 미디엄. **3/4 정면**이다.
   *
   * ⚠ 처음엔 카메라를 주인공 **뒤쪽**(서쪽)에 뒀다. 주인공이 동쪽을 보고 앉아
   *   있으니 화면 한복판이 뒤통수와 등받이가 됐고, 그 한 장으로는 아무것도
   *   전달이 안 됐다 — "버스를 타고 역으로 가는 사람"이 안 읽힌다.
   *
   * 그래서 **앞쪽(동쪽) 통로**로 옮겨 돌아본다. 그러면
   *   · 얼굴·상체·팔·다리와 좌석이 한 화면에 들어오고
   *   · 창(북)이 인물 뒤에 놓여 지나가는 도심이 배경이 되며
   *   · 앞좌석 등받이와 봉이 전경으로 깔린다
   * 스토리보드 첫 컷의 구도가 정확히 이것이다.
   */
  if (t < SHOT.interior) {
    const p = framed(
      [SEAT.x + 1.66, SEAT.y - 1.08, 1.46],
      [SEAT.x + 1.50, SEAT.y - 1.01, 1.42],
      [SEAT.x + 0.02, SEAT.y + 0.02, 1.12],
      seg(t, 0, SHOT.interior), dx,
    )
    return { ...p, eye: p.eye + shake * 0.02, pitch: p.pitch + shake * 0.005 - brakeDip(t) }
  }

  /**
   * ── ② 휴대폰 OTS. 오른쪽 어깨 너머다.
   *
   * 주인공이 동쪽을 보고 앉아 있으므로 **뒤 = 서쪽 · 오른쪽 = 남쪽**이다.
   * 클로즈업으로 화면만 꽉 채우지 않는다 — 인물이 남아 있어야 같은 버스 안이라는
   * 것이 유지된다(브리프 §17).
   *
   * ⚠ 어깨 **높이**에 두면 화면 뒷면만 보인다. 휴대폰 화면은 주인공의 얼굴을
   *   향하므로(실측 법선: 월드 −0.75, +0.66, −0.05 = 뒤·위), 카메라도 그 법선을
   *   따라 **머리 위쪽**에 서야 화면이 읽힌다. 어깨 너머로 같이 내려다보는 각이다.
   *
   * ⚠ 그런데 머리 **바로 뒤**에 두면 머리가 화면 절반을 먹고 폰보다 커진다.
   *   머리를 **옆으로 지나쳐** 폰 쪽으로 붙는다 — 어깨와 머리 일부는 전경에
   *   남고, 초점은 손과 화면으로 간다.
   *
   * ★ 겨냥점과 거리는 **실측한 폰 위치**에서 왔다. 폰은 앉은 주인공의 가슴 앞
   *   0.19m · 높이 1.02 에 온다(어깨 1.26 · 팔꿈치 1.04). 거기서 0.5m 떨어져
   *   보면 실물 스마트폰(0.082 × 0.168)도 화면에서 충분히 읽힌다 —
   *   **가독성은 폰을 키워서가 아니라 카메라를 붙여서** 얻는다.
   *
   * ⚠ 시선을 막는 것이 둘 있었다.
   *   · 뒤에서 눈높이로 잡으면 **자기 좌석 등받이**(위 끝 1.25)가 가린다.
   *   · 오른쪽 뒤로 비키면 이번엔 **폰을 든 오른팔 자체**가 가린다.
   *
   *   폰은 가슴 앞이고 화면은 얼굴을 본다. 그러면 그 화면이 보이는 자리는 하나뿐
   *   이다 — **머리 바로 위를 지나 내려다보는 각.** 1.64 에서 겨냥선이 정수리
   *   (1.27)를 6cm 차이로 넘어간다. 머리와 어깨는 전경 아래에 남는다.
   */
  /**
   * ★ 이 샷은 **밀고 들어간다**(push-in). 다른 샷의 dolly 가 3~5cm 인 것과 다르다.
   *
   * 이유는 가독성이다. 0.72m 에서 폰 화면은 세로 113px(720 중)이고, 그 안의
   * 「분」은 12px 이 된다 — 한글 12px 는 1.4초 안에 안 읽힌다(`phone.ts` 의
   * 환산 참고). 0.44m 까지 붙으면 폰이 185px 이 되어 「분」이 24px 이 된다.
   *
   * ⚠ 그렇다고 처음부터 0.44m 에 세우면 **인물이 사라지고 폰만 남는다** —
   *   그건 브리프가 금지한 "공중에 뜬 전광판"과 같은 그림이 된다(§17: 인물이
   *   남아 있어야 같은 버스 안이라는 것이 유지된다).
   *
   * 그래서 **처음에는 OTS, 끝에는 인서트**다. 첫 프레임은 어깨·머리·폰이 다
   * 들어오는 구도로 "버스에 앉아 폰을 본다"를 성립시키고, 밀면서 화면이 커져
   * 컷이 끝나기 전에 「3분 후」가 확실해진다. 사람이 실제로 시간을 확인할 때
   * 눈이 하는 일과 같은 순서다.
   */
  if (t < SHOT.phone) {
    const p = framed(
      /**
       * ★ **카메라를 인물 뒤로 옮겨 동쪽을 보게 한다** — 창밖이 프레임에 없었다.
       *
       * 이전 값(+0.18·+0.32)은 인물의 **앞**(동쪽)에서 서북서를 보는 자리였다.
       * 그 방향에는 북측 옆판밖에 없어서, 화면의 절반이 아이보리 벽이고 창밖은
       * 왼쪽 위 구석에 겨우 걸렸다. "버스 안에서 이동 중"이 시각적으로 성립하지
       * 않는다 — 벽 앞에 앉은 사람과 구분이 안 된다.
       *
       * 주인공은 북측 1인석(y 21.20)에 앉아 **동쪽을 본다**(실측: 발 x −60.32 >
       * 엉덩이 −60.57). 그러므로 창(북측, z 1.24~2.43)은 인물의 **왼쪽**이고
       * 통로와 남측 2인석은 **오른쪽**이다. 카메라를 뒤(서)로 빼서 동쪽을 보면
       * 그 배치가 한 화면에 들어온다 — 왼쪽에 어깨와 창, 가운데에 손과 폰,
       * 오른쪽에 통로·파란 좌석·노란 봉이 소실점으로 물러난다.
       *
       * ■ 자리를 정하면서 막힌 것 둘 — **둘 다 카메라로는 못 풀었다**
       *
       * ⚠ 정후방(서쪽 0.78m)에 세웠더니 **좌석 등받이와 뒤통수가 폰을 통째로**
       *   가렸다. 등받이는 좌석 중심에서 서쪽 0.225m 에 있고 높이 1.25m 다 —
       *   그 뒤에 서면 앉은 사람의 앞을 볼 방법이 없다. 통로 쪽(남 0.46m)으로
       *   돌려 등받이 **위**를 넘어 보는 자리로 옮겼다.
       *
       * ⚠ 그 다음엔 **몸통이** 폰을 가렸다. 이건 자세 문제였다 — `pose.ts` 의
       *   `FOREARM_IN_R` 이 폰을 몸 중심선까지 끌어와 놨다. 카메라를 옮겨 봐야
       *   실루엣 안에 있는 물건은 안 보인다. 그쪽을 고쳤다(해당 주석 참고).
       *
       * z 1.34 는 **창 하단(1.24)보다 위**다. 이보다 낮으면 시선이 허리 판을 보고
       * 창밖이 안 들어온다. 반대로 내려다보는 각을 12° 까지 눕혀 봤더니 천장과
       * 평평한 옆판만 늘고 창밖은 오히려 줄었다 — 19° 가 그 사이다.
       */
      [SEAT.x - 0.28, SEAT.y - 0.56, 1.44],
      [SEAT.x - 0.10, SEAT.y - 0.46, 1.36],
      /**
       * 겨냥점은 폰(z 1.02)과 머리(z 1.12) **사이**다. 폰만 겨냥하면 시선이 너무
       * 내려가 머리가 화면 위로 잘린다 — 어깨와 머리 일부가 남아야 OTS 다.
       */
      /**
       * 밀고 들어간 뒤에는 겨냥점이 **4cm 올라가 있어야** 머리가 프레임에 걸린다.
       * 1.13 에서는 다 붙었을 때 턱까지만 남고 머리가 위로 잘려서, 인물이 있는
       * OTS 가 아니라 **폰만 있는 인서트**로 보였다. 폰은 0.42m 앞이라 이만큼
       * 올려도 화면 아래에 넉넉히 남는다.
       */
      /**
       * ★ 겨냥점을 **폰으로** 옮겼다.
       *
       * 예전 값(+0.13, −0.18, 1.17)은 폰이 몸 중심선에서 20cm 오른쪽·팔꿈치 높이에
       * 있던 시절에 맞춘 것이다. 팔을 몸 쪽으로 모으고 들어 올린 뒤(`pose.ts` 의
       * `FOREARM_IN_R`) 폰은 (+0.08, −0.08, 1.09) 로 왔다 — 옛 겨냥점은 그보다
       * 0.10m 오른쪽이라 **화면 초점이 폰이 아니라 그 옆 허공**이었다.
       *
       * 폰과 가슴 사이를 본다. 폰이 화면 가운데로 오고, 어깨와 팔은 왼쪽에 남는다.
       */
      /**
       * 겨냥점은 폰(z 1.09)보다 조금 위다. 폰을 정확히 겨냥하면 시선이 내려가
       * 창밖이 프레임 위로 빠진다 — 폰은 화면 가운데 아래에 남으면 된다.
       */
      [SEAT.x + 0.24, SEAT.y - 0.20, 1.15],
      seg(t, SHOT.interior, PUSH_END), dx,
    )
    return { ...p, eye: p.eye + shake * 0.015, pitch: p.pitch + shake * 0.004 - brakeDip(t) }
  }

  /**
   * ── ③ 버스 외부 · 문. **기존 버스 외피를 그대로 보여 주는 샷이다.**
   *
   * 인도 위, 쉘터(x −60~−56 · y 23.4~25.4) **바깥**에 선다. 거기서 서쪽을 보면
   * 버스 옆면과 뒷문(−60.3)이 한 화면에 들어오고, 주인공이 그 문에서 나온다.
   * "아까 그 버스에서 내리는구나"가 이 한 장으로 읽혀야 한다.
   *
   * ⚠ 처음엔 쉘터 **동쪽**에 세웠다. 두 번 다 유리 패널이 화면을 덮어 버스가
   *   회백색 판때기로 보였다 — `__game.pick()` 으로 찍으니 2.5~3m 앞의
   *   `merged:BLD_GLASS`, 쉘터였다. 눈으로만 보면 "실내가 외피를 뚫었나" 로
   *   헛짚는다.
   *
   *   쉘터는 x −60~−56 · y 23.4~25.4 다. 그 **서쪽**에서 동남동을 보면 시선이
   *   문(−60.3)에 닿을 때까지 쉘터 x 범위에 안 들어간다.
   *
   *   거기서 한 번 더 조였다 — 뒤로 물러서 있었더니 광고판이 화면 왼쪽 1/3 을
   *   먹고 주인공이 작았다. 연석 쪽(y 23.0)으로 내려오고 2m 까지 붙이면 광고판이
   *   화면 밖으로 밀려나고 문에서 내리는 사람이 주인공 크기로 잡힌다.
   */
  if (t < SHOT.door) {
    /**
     * ⚠ 문이 1.4m 동쪽으로 갔으므로 카메라도 같이 옮긴다. 그러지 않으면 문까지
     *   3.7m 가 되어 내리는 사람이 작아진다.
     *
     * ⚠ 쉘터(x −60~−56 · y 23.4~25.4) 유리에 가릴 위험이 있는 자리다. 다만
     *   카메라와 겨냥점이 **둘 다 y 23.15 이하**라 시선이 쉘터의 y 범위(23.4~)에
     *   들어가지 않는다 — x 가 겹쳐도 안 가린다. `pick()` 으로 확인했다.
     */
    return framed(
      [-61.1, 23.15, 1.78],
      [-60.7, 22.95, 1.72],
      [DOOR_X + 0.10, 22.35, 1.12],
      seg(t, SHOT.phone, SHOT.door),
    )
  }

  /**
   * ── ④ 질주 팔로우 → 1인칭.
   *
   * 뒤에서 따라가다 **머리 속으로 들어가** 그대로 조작권이 된다. 거리가 0 이 되는
   * 순간이 곧 시점 전환이라 이음매가 없다. 마지막 값은 `FINAL_POSE` 와 같아야 한다.
   */
  const u = seg(t, SHOT.door, INTRO_MS)
  const close = closeAt(t)
  const k = 1 - close
  const camX = a.x + FOLLOW.e * k
  const camY = a.y + FOLLOW.n * k
  // 카메라를 낮춘다 — 눈높이보다 아래에서 올려다보면 달리는 속도가 커 보인다
  const camZ = lerp(FPV.eyeHeight, a.z + 1.12, k)
  const aimZ = a.z + 0.95
  const ddx = a.x - camX
  const ddy = a.y - camY
  const dist = Math.hypot(ddx, ddy)
  const lookYaw = dist > 1e-4 ? Math.atan2(ddy, ddx) : 0
  const lookPitch = dist > 0.05 ? Math.atan2(aimZ - camZ, dist) : 0
  /** 속도감은 거리가 아니라 화각이 만든다. 끝에서 정확히 기본값으로 닫힌다 */
  const punch = Math.sin(clamp01(u * 1.2) * Math.PI)
  return {
    x: camX,
    y: camY,
    eye: camZ + Math.sin(u * Math.PI * 6) * 0.02 * Math.sin(u * Math.PI),
    yaw: lerp(lookYaw, 0, close),
    pitch: lerp(lookPitch, 0, close),
    fov: FPV.fovDeg + punch * 9,
  }
}
